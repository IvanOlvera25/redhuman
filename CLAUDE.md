# Red Human AI — Reglas del proyecto

Plataforma SaaS de agente de IA de RH para México. `red-human-app` (Next.js 15) + `red-human-api` (FastAPI). Respuestas y UI en español mexicano.

## Claves y seguridad

- Las claves viven SOLO en `red-human-api/.env` (gitignored). Nunca en código, ni en el bundle del cliente, ni pegadas en chats.
- `OPENAI_API_KEY` → cerebro de toda la IA (modelo en `OPENAI_MODEL`, hoy `gpt-5.6-luna`).
- `ANAM_API_KEY` → avatar de entrevistas. NUNCA exponerla al navegador: los session tokens se generan en el servidor (`app/services/avatar.py`) y solo el token efímero (~1 h) llega al cliente.
- El endpoint de OpenAI está registrado en Anam como LLM propio (`ANAM_LLM_ID`); para rotar la clave de OpenAI hay que actualizar también esa registración (`POST https://api.anam.ai/v1/llms`).
- Todo `personaConfig` inline debe incluir `llmId`, o Anam rechaza el token como "legacy".

## Avatar de entrevistas (Anam)

- Anam escucha (STT), habla (TTS) y pone la cara; el que piensa es NUESTRO modelo de OpenAI.
- El endpoint del LLM debe soportar streaming.
- Respuestas del agente breves y conversacionales: se dicen en voz alta.
- Preparar en page-load (SDK precargado, `preconnect` a api.anam.ai); el clic del usuario solo inicia el stream.
- Usar avatares neutrales del catálogo de Anam — nunca clones de personas reales sin consentimiento expreso (reforma de derecho de autor 2026).

## WhatsApp (Meta Cloud API)

- `WHATSAPP_PROVIDER=meta`. El token (`META_WHATSAPP_TOKEN`) vive solo en el servidor; nunca al navegador.
- El webhook `POST /webhooks/whatsapp` es público: valida siempre la firma `X-Hub-Signature-256` con `META_APP_SECRET`.
- El webhook contesta 200 de inmediato y corre el prefiltro en segundo plano — Meta reintenta si tardas, y se deduplica por `wamid`.
- Teléfonos: en la base se guardan a 10 dígitos; hacia la API salen como `52` + 10 (sin el `1`, que Meta ya no usa).
- Ventana de 24 h: fuera de ella Meta rechaza el texto libre (error 131047) y hay que usar plantilla aprobada (`META_PLANTILLA_AVISO`).
- Ningún mensaje saliente decide nada: sigue siendo la persona de RH quien avanza o descarta.

## Legal (México)

- LFPDPPP 2025: la IA solo recomienda; avanzar/descartar/alta siempre lo decide una persona de RH con nombre registrado en bitácora (human-in-the-loop).
- Consentimiento explícito del candidato antes de cualquier entrevista o tratamiento de datos; queda en la bitácora hash-encadenada.
- No pedir ni inferir datos sensibles (salud, embarazo, religión, estado civil, orientación).

## Fase 2 — Candidato (persona) vs Postulación (proceso)

- `Candidato` = identidad (nombre, teléfono, correo, wa_id, CV, archivos). `Postulacion` = una aplicación a una vacante: etapa, estado, score, chat, entrevistas, expediente. **Ningún endpoint escribe estado de proceso en `Candidato`** (sus columnas de proceso son legado solo para `scripts/migrar_postulaciones.py`).
- Kanban: una tarjeta por Postulación; `id`/`candidatoId` en la API es el código `P-####` (lo que se manda a `/candidatos/{codigo}/...`); la persona va en `candidatoCodigo`. Se acepta `C-####` por compatibilidad.
- Expediente pertenece a la Postulación (una persona puede tener varios a lo largo del tiempo).
- Reaplicar: si hay postulación ACTIVA para esa vacante se reutiliza; si está cerrada (`descartado`/`contratado`/`reinicio_prueba`) se crea una nueva y la vieja queda como historial. `descartar` cierra; mover de etapa reabre.
- WhatsApp: elegir vacante del menú NUNCA es consentimiento — siempre aviso de privacidad + "Sí"/"Acepto" explícito (palabra completa, `_es_aceptacion`) antes de iniciar el prefiltro; solo se omite si la postulación ya tenía consentimiento registrado por otro medio (RH / `/aplicar`).
- WhatsApp: `Candidato.postulacion_conversacion_id` es la postulación en chat. Lo mueve SOLO el candidato (mensaje entrante enrutado o selección explícita en lista); un mensaje saliente de RH/sistema NUNCA lo mueve (B1). Si el puntero no sirve y varias postulaciones esperan respuesta (`Postulacion.espera_respuesta`), el webhook manda lista interactiva (ids `P-####`) y NO adivina. Ver docstring de `routers/webhooks.py`.
- Crear postulaciones SOLO con `candidatos.crear_postulacion` / `postulacion_para_vacante` (código `P-{8800+id}` de la propia postulación).
- Kanban (B4): `GET /candidatos` regresa solo postulaciones activas salvo `mostrar_cerradas=true` (toggle "Mostrar cerradas") o `activa=` explícito.
- Base existente: correr `scripts/migrar_postulaciones.py --forzar` una vez ANTES de arrancar la versión nueva; la API se niega a arrancar (RuntimeError en lifespan) si hay candidatos sin postulación — la migración de datos nunca corre sola. Prueba de regresión: `scripts/verificar_fase2.py` (modo demo, base desechable).

## Configuración administrativa (Puntos 9-13)

- Cuentas: un Administrador solo ve/administra las Cuentas vinculadas a él (`usuario_cuentas`); `GET/PATCH /cuentas/{id}` responde 404 para una ajena. `Cuenta.nombre` (interno) ≠ `nombre_comercial` (candidatos); leer siempre `nombre_visible`.
- Notificaciones: `services/notificaciones.disparar(..., override=)` acepta ajustes SOLO para esa acción (`NotificarIn`, body `notificar`); la regla guardada nunca se toca desde una acción. Los eventos automáticos (`candidato_apto`, no-show, liga externa) nunca mandan override. En el frontend, toda acción manual que notifica pasa por `LineaNotificar`/`ConfirmacionAccion`.
- Plantillas: el contenido compartido Vacante↔Plantilla es `models.CAMPOS_PLANTILLA` (única lista); el formulario de contenido es uno solo (`components/dashboard/vacantes/formulario-contenido.tsx`) para Nueva vacante y Configuración → Plantillas.
- Modo Prueba: la ventana de nueva sesión se lee de `ConfiguracionSistema.modo_prueba_ventana_min` (nunca hardcodearla).
- Al cambiar de Cuenta, `<PorCuenta>` (layout del dashboard) remonta todo el árbol: no cachear datos por Cuenta fuera de React.

## Entrevista IA (Fase 4)

- Toda identidad de empresa en contenido generado por IA (vacantes, WhatsApp, entrevista) sale de UNA regla: `serial.nombre_empresa(cuenta, cliente, mostrar)` / `nombre_empresa_candidato(v)` → Cliente visible si "mostrar cliente al candidato", si no el nombre comercial de la Cuenta. Nunca texto libre.
- Nombre del candidato: siempre el de la ficha (`candidatos.nombre_ficha(p)`); el perfil de WhatsApp (`wa_nombre`) solo si la ficha trae placeholder. Contexto de la entrevista siempre desde `e.postulacion` (nunca desde la persona).
- La entrevistadora se presenta SOLO como «Red Human» (nunca con nombre de persona — "Alma" se eliminó —, nunca "asistente virtual" ni "agente de inteligencia artificial"; si le preguntan si es IA lo dice con naturalidad). Introducción EXACTA en `ia.mensaje_inicial_entrevista(titulo_vacante)` con el nombre real de la vacante; tras la respuesta del candidato inicia de inmediato sin transición; silencio `ia.AVISO_SILENCIO`; sin numerar preguntas; una pregunta principal por turno; despedida fija `ia.DESPEDIDA_ENTREVISTA`. El aviso "conversas con una IA" vive solo en la pantalla de consentimiento (LFPDPPP).
- Cierre verificable: el backend solo cierra una entrevista con `POST /finalizar` y verifica el `cierre` declarado contra el transcript (`_cierre_verificado`); sin señal verificable degrada a `manual`; desconexión con <2 turnos → `interrumpida` (RH reabre con `POST /entrevistas/{codigo}/reabrir`). Nunca cerrar por tiempo en el servidor sin señal.
- Enfoque de entrevista por vacante: SOLO 2 niveles (`profesional` | `profesional_personal`, `models.ENFOQUES_ENTREVISTA`). No agregar más.
- Datos sensibles (`ia.DATOS_SENSIBLES_PROHIBIDOS`): ni preguntar ni registrar aunque el candidato los mencione — aplica al entrevistador y al evaluador.

## Formulario de vacante (Parte 3)

- Orden fijo del formulario compartido (`components/dashboard/vacantes/formulario-contenido.tsx`, Nueva vacante Y Plantillas): Datos principales → Guía opcional → [Generar vacante con Red Human] → Contenido generado y editable → Selección (Prefiltro → Entrevista Red Human) → Gestión → Publicación. El botón de generar va ABAJO de la captura, nunca arriba. No existe "Notas para la IA".
- Regla no negociable 1: Red Human NUNCA inventa condiciones reales (sueldo, periodicidad, ubicación, modalidad, horario, prestaciones). Lo que RH no capturó queda vacío/pendiente con aviso; `beneficios` = exactamente los capturados; `rango_salarial_sugerido` es solo informativo. Garantizado en `ia._asegurar_capturado` (IA y demo) — no depende del prompt.
- Regla no negociable 2: lo capturado se respeta literal — indispensable sigue indispensable, deseable sigue deseable, seniority el elegido; la IA solo AGREGA/complementa lo vacío (`_unir_capturado`, `contenidoDesdeGenerado`). La descripción breve es guía y se expande.
- Sueldo: se captura ESTRUCTURADO (`sueldo_desde/hasta/moneda/periodicidad`, `PERIODICIDADES_SUELDO`); `Vacante.sueldo` (texto) es DERIVADO por `models.texto_sueldo` y es lo que leen WhatsApp, prefiltro, entrevista, portal, publicaciones y el agente. Nunca capturar ni editar el texto por separado. Seniority = uno de `ia.SENIORITY`.
- `Vacante.requisitos` sigue siendo texto (indispensables unidos por « · »); usa `routers.vacantes.requisitos_lista` / `requisitosLista` para tratarlo como lista.
- "Entrevista IA" es el valor interno de la etapa (base y API); en la interfaz siempre se muestra con `nombreEtapa()` → «Entrevista Red Human». No renombrar el valor almacenado.

## Entrevista Humana (Fase 7A)

- Entrevistador interno = `Usuario` de la Cuenta: nombre, correo y WhatsApp salen SIEMPRE del perfil (`Usuario.telefono`, capturado en Configuración → Usuarios); nunca se recapturan en la entrevista. `/auth/entrevistadores` los expone a cualquier sesión de la Cuenta.
- Entrevistador externo = `ClienteContacto` del Cliente de la vacante (`EntrevistaHumanaIn.entrevistador_contacto_id`, se guarda `EntrevistaHumana.contacto_id` y se copian nombre/correo/WhatsApp) o «+ Otro entrevistador» (captura manual). Nunca limitar a contactos registrados.
- Notificar al Cliente: `NotificarIn.cliente_contactos_ids` elige contactos ya registrados (None = todos, [] = ninguno); nunca se capturan datos nuevos del Cliente en una acción.
- Correo: `services/correo.py` (Resend). Sin `RESEND_API_KEY` no sale y se registra «RESEND_API_KEY sin configurar». 2026-09-18: el remitente es ESTRICTAMENTE `@redhuman.mx` (`correo.remitente()`: default `notificaciones@redhuman.mx`; un `RESEND_FROM` de otro dominio se ignora con warning) — el dominio debe estar verificado en Resend. Todo envío regresa `{destinatario, canal, destino, enviado, detalle}` y las acciones manuales lo muestran a RH (`lineasResultados`): un envío fallido nunca es silencioso.
- Las reglas de notificación de una Cuenta NACEN con `models.REGLAS_NOTIFICACION_DEFAULT` (entrevista_agendada = correo+WhatsApp a candidato y entrevistador); las reglas ya guardadas nunca se tocan automáticamente.

## Microsoft Teams (Fase 7B)

- Variables EXACTAS: `TEAMS_CLIENT_ID`, `TEAMS_TENANT_ID`, `TEAMS_CLIENT_SECRET` (+ opcional `TEAMS_REDIRECT_URI`). Solo en el `.env` del servidor; sin ellas `teams.teams_configurado()` es False y todo cae a la liga manual (mismo patrón que Resend/Anam: nunca rompe el flujo).
- OAuth con permisos DELEGADOS, conexión POR CUENTA (`IntegracionTeams`, una fila por Cuenta). Tokens SIEMPRE cifrados (Fernet con clave derivada de `TEAMS_CLIENT_SECRET`, `services/teams.py`); nunca se exponen por la API. Rotar el secret obliga a reconectar.
- La reunión se crea con `POST /me/events` (`isOnlineMeeting` + `teamsForBusiness`, asistentes = correos de candidato y entrevistador): Graph manda las invitaciones de calendario. Se crea ANTES de guardar la entrevista; si falla → 502 y no se guarda nada (decisión del usuario). Modificar/cancelar sincronizan la reunión best-effort (nunca bloquean).
- Con Teams conectado la UI NO pide la liga en Videollamada; «Usar otra liga» (`usar_teams=false`) es la excepción. No agregar modalidades ni pasos.

## Evaluación del candidato (2026-09-13)

- Prefiltro por WhatsApp = SOLO filtro de entrada: resultado `cumple` | `no_cumple`, sin score. `Postulacion.score` lo escribe únicamente el Análisis de CV. El prefiltro NO participa en la evaluación integral ni en la afinidad.
- Evaluación integral (`ia.evaluar_entrevista`) = Análisis de CV + Entrevista Red Human, nada más. Estructura: Afinidad (`match_perfil`), Fortalezas, Puntos por validar (`riesgos`), Recomendación, `faltante`.
- `/finalizar` valida el transcript antes de evaluar: sin respuestas útiles → `interrumpida` (`motivo=sin_respuestas`); pocas respuestas → `ia.suficiencia_entrevista` decide `parcial` (sin score) o evaluar indicando lo que faltó. Nunca generar score ni recomendar entrevista humana sin entrevista válida; la acción siguiente es «Reintentar Entrevista Red Human» (reabrir).
- La sala pública nunca debe quedar en negro: errores de sesión → fase `cerrada`/`error` con mensaje; fallo del avatar → sesión de texto forzada (`POST /sesion {"modo":"texto"}`); `error.tsx` en la ruta.
- Embudos y contadores por etapa cuentan solo postulaciones activas.

## Correcciones post-demo (2026-09-15)

- Memoria de WhatsApp: en `ETAPAS_CONTEXTO_LARGO` (Entrevista IA → Onboarding) el contexto vive `CONTEXTO_WHATSAPP_HORAS` (120 h); la ventana corta de Modo Prueba solo aplica a Prefiltro. La frialdad se mide sobre la POSTULACIÓN (`webhooks._ultima_actividad`: creación / `ultima_actividad_en` / último mensaje). Pasados los 5 días en Modo Prueba se cierra por `sin_interes`; con Modo Prueba apagado nunca se cierra sola.
- Reagendar: con cita agendada, "reagendar/cambiar/no pude…" o un «Sí» tras el aviso de no-show reabre la agenda en la MISMA postulación (`candidatos._quiere_reagendar` → `_reabrir_agenda`; la cita anterior queda en `analisis.videollamadas_anteriores`). Nunca respuesta fija ni menú.
- No-show: `services/agenda.py` reclama el flag con UPDATE condicional + commit ANTES de enviar y registra `NotificacionEnviada(evento="reagendar_noshow")`; no se repite en 24 h aunque el flag se pierda. El job va con `max_instances=1, coalesce=True`.
- Alta como colaborador: sin NINGÚN documento adjunto → 400 «El expediente no tiene documentos adjuntos» — SOLO con Modo Prueba apagado (2026-09-18: Modo Prueba TOTAL omite toda la integridad).
- Documentos por WhatsApp: `META_PLANTILLA_DOCUMENTOS` (default `solicitud_documentos_rh`) con variables en el orden de `META_PLANTILLA_DOCUMENTOS_PARAMS` (`nombre,documentos,liga[,empresa,vacante]`); si falla, texto libre. «Solicitar documentos» / «Enviar recordatorio» existen en Contratación y Onboarding. Un `image`/`document` entrante en esas etapas se descarga de Graph (`whatsapp.descargar_media`) y se adjunta al expediente (`contratacion.documento_para_adjunto` + `adjuntar_documento_bytes`, misma validación que la liga pública). Regresión: `scripts/verificar_correcciones_demo.py`.

## Fases 1-3 post-demo (2026-09-15)

- Cuentas: baja lógica (`estado="Eliminada"`, `eliminada_en/por`; `DELETE /cuentas/{id}`, `POST /{id}/restaurar`); nunca la actual ni la última activa. Predeterminada es POR USUARIO (`Usuario.cuenta_predeterminada_id`); `cuenta_actual` sin cabecera cae en ella.
- Cargas masivas: `services/masivo.leer_tabla` (CSV/xlsx, encabezados homologados) + savepoint por fila; los endpoints `/auth/usuarios/masivo`, `/clientes/masivo`, `/plantillas/masivo` reutilizan SIEMPRE la función del alta individual (`crear_usuario_basico`, `_crear_cliente`, `_crear_plantilla`). `GET …/masivo/plantilla` = CSV de ejemplo.
- Entrevista IA interrumpida/parcial: el candidato la reanuda desde WhatsApp (`candidatos._reanudar_entrevista_ia` → `services.entrevistas.reabrir_entrevista`, misma liga) o la reagenda (`_reabrir_agenda`). Una `evaluada` solo la reabre RH.
- Recordatorios automáticos de documentos: `services/recordatorios.py` (cada hora) manda `recordatorio_documentos` cada `ConfiguracionSistema.recordatorio_documentos_dias` desde `recordatorio_documentos_hora` (MX) mientras `Expediente.documentos_hasta` no venza; al vencer, un solo aviso en bitácora. Reclama `ultimo_recordatorio_en` antes de enviar.
- `notificaciones.disparar` sin regla guardada usa `REGLAS_NOTIFICACION_DEFAULT` (eventos automáticos en Cuentas nuevas ya no se quedan mudos).
- `Expediente.progreso` cuenta digitales subidos (`Documento.entregado`); el alta exige confirmación de RH (`sin_confirmar`). Carta de intención con fpdf2 (`services/pdf.py`), no WeasyPrint.
- Fase 4: ubicación de vacante/plantilla estructurada (`ubicacion_estado`, `ubicacion_municipio`, catálogo INEGI en `red-human-app/lib/estados-municipios.json`); `Vacante.ubicacion` (texto) se DERIVA con `models.texto_ubicacion` (texto libre solo para registros previos). Prefiltro por WhatsApp = `preguntas_filtro_whatsapp` (independiente de `preguntas_filtro`, la web); vacío → el agente usa las de la web. El botón «Generar vacante» va al FINAL del formulario (después de Prefiltro y Entrevista). Tableros se revalidan con `lib/use-polling.ts` (`usePolling`), nunca con timers a mano.
- Fase 5: `GET /colaboradores?cliente_id=` (0 = directo) + `GET /colaboradores/clientes` alimentan el filtro por Cliente del tablero (`clienteNombre` = `Cliente.nombre` interno). Al dar de alta (`contratacion.alta`) se dispara SIEMPRE el evento automático `instrucciones_ingreso` (bienvenida + datos + `Expediente.instrucciones_ingreso`, regla default correo+WhatsApp, sin override); cuando sale, el evento `contratacion` deja de escribirle al candidato para no duplicar (sigue avisando al Cliente).
- CRUD (2026-09-15): eliminar es SIEMPRE baja lógica. Vacante → `estado="Eliminada"` + `eliminada_en/por` (`DELETE /vacantes/{codigo}`, `POST /{codigo}/restaurar`); cierra sus postulaciones activas (`vacante_eliminada`), sale de listados/portal/WhatsApp/métricas, y `_no_eliminada` bloquea editar/publicar/cerrar/regenerar. Candidato → `Candidato.eliminado_en/por` (`DELETE /candidatos/{codigo}`, acepta P-#### o C-####, elimina a la PERSONA): cierra sus postulaciones (`eliminado`), 404 en `_por_codigo`, excluido del Kanban, de `_duplicado` y del webhook (si vuelve a escribir nace una persona nueva); bloqueado si ya es colaborador activo. Editar vacante = formulario compartido en modo edición (`contenidoDesdeVacante` → `PATCH /vacantes/{codigo}`).
- Arranque en vivo (2026-09-15): `app/seed.py` NO siembra datos de ejemplo salvo `SEMBRAR_DEMO=true` (los scripts de verificación lo activan); base ya sembrada → `scripts/limpiar_datos_demo.py --forzar` (baja lógica). Los tableros del frontend arrancan VACÍOS con esqueleto de carga — nunca con mocks de `lib/data.ts` (`candidatosDemo`, `vacantesDemo`, `nuevosIngresos`). `GET /contratacion/expedientes` excluye personas eliminadas y postulaciones cerradas (salvo `contratado`). Colaborador: baja (`POST /colaboradores/{codigo}/baja`, reversible) ≠ eliminación lógica (`DELETE`, fuera de listados/conteos). Sidebar «Agente activo» lee `GET /candidatos/agente/actividad`.

## Reglas de UI (reestructura 2026-09-16)

- Una sola acción principal visible por contexto; las secundarias van en `components/dashboard/menu-acciones.tsx` (`MenuAcciones`, menú «…»). Carga masiva = solo ícono con tooltip. Lo opcional vive en acordeones CERRADOS; sin "efecto libro". La IA automatiza primero; RH edita solo si quiere.
- Formulario de vacante: Datos principales (condiciones reales) → «Sobre la vacante» (descripción breve, indispensables, deseables, prestaciones, enfoque) → botón «Generar vacante con Red Human» → acordeón «Configuración avanzada» cerrado (responsabilidades, resumen, perfil ideal, palabras clave, prefiltro web, prefiltro WhatsApp, textos).
- Modo Prueba flexible: `services/configuracion.permite_duplicados(db)` — con Modo Prueba activo, teléfono/correo se repiten entre candidatos sin fusionar ni bloquear (cada alta es persona nueva); apagado → dedupe normal (nunca 409). El correo de login de `Usuario` es único SIEMPRE.
- Prefiltro dual (F2): la IA genera `preguntas_filtro` (web) y `preguntas_filtro_whatsapp` (2-3 puntos críticos, `ia.puntos_criticos_whatsapp` como fallback). `/postular` guarda `analisis.respuestas_web`; `candidatos.comparar_web_vs_whatsapp` detecta contradicciones → el agente manda UNA pregunta de aclaración (`aclaracion_pendiente`) y el siguiente turno la registra en `analisis.inconsistencias`. Con contradicción registrada la IA nunca descarta: un cierre `no_cumple` se convierte en `revision` para RH.
- Control manual (F2): `PATCH /candidatos/{codigo}/etapa {manual:true}` mueve a cualquier etapa (incluida Entrevista Humana sin agendar y Onboarding directo) y registra lo saltado en `Postulacion.actividades_omitidas` (usuario, fecha, motivo). Fallas de WhatsApp/correo nunca bloquean el movimiento. En la ficha: UNA acción principal + `…` con «Mover a otra etapa». Detalle de vacante = resumen + secciones plegadas (`Plegable`).
- Capacitación universal (F3): UN solo módulo y tipo de curso (`Curso` con `evaluacion` JSON integrada, `calificacion_minima`, `adjuntos`, `contexto`). Crear = tema + contexto opcional + adjuntos + duración → `ia.guion_curso` genera objetivo, categoría, módulos y evaluación (opción múltiple / V-F; `_asegurar_evaluacion` la garantiza). `AsignacionCurso.tipo` ∈ colaborador | candidato (`postulacion_id`) | externo (`externo_*`, liga abierta pide nombre + correo/WA en `/publica/{token}/registro`). Sala pública: `/avanzar` módulo por módulo, `/responder` una pregunta a la vez (nunca expone `correcta`), resultado Aprobado/No aprobado + % → `analisis.capacitacion` del candidato. `Vacante.curso_filtro_id` se asigna solo al quedar apto (`candidatos._asignar_curso_filtro`). Tablero único `GET /capacitacion/asignaciones` con filtros. Nada de plataformas externas ni módulos separados de evaluación.
- WhatsApp y vacantes (2026-09-16): el agente lee las vacantes de la base en CADA turno (`webhooks._vacantes_publicadas`, solo `Publicada`; no existe caché). Con varias Cuentas activas y ninguna con `whatsapp_comunicacion` igual al número receptor, `_cuenta_whatsapp` elige: proceso activo de esa persona (postulación activa CON vacante) → Cuenta con vacantes publicadas → la más antigua. Nunca quedarse fija en la Cuenta más antigua. Selección por código/lista (`_vacante_por_codigo`) y `/candidatos/postular` (410) solo aceptan vacantes Publicadas: una lista o liga vieja de una vacante eliminada/cerrada no abre postulaciones. Regresión: `scripts/verificar_whatsapp_vacantes_nuevas.py`.

## Bugs 2026-09-17 (ligas, Entrevista IA, ficha, portal)

- Agenda por WhatsApp: la liga de la entrevista sale UNA vez — `candidatos._sin_ligas` quita cualquier URL escrita por el modelo antes de anexar `turno.cita_liga`. Nunca confiar en que el prompt evite la copia.
- Entrevista IA durable: `POST /entrevistas/publica/{token}/transcript` (el navegador lo manda con debounce en cada `MESSAGE_HISTORY_UPDATED`; nunca acorta lo guardado), `Entrevista.ultima_actividad_en`, `/finalizar` evalúa el transcript MÁS LARGO entre cliente y servidor (`_transcript_mas_completo`), el navegador toma snapshot ANTES de `stopStreaming` y manda `fetch keepalive` en `pagehide`; job `cerrar_entrevistas_inactivas` (5 min; `INACTIVIDAD_ENTREVISTA_MIN`=15) cierra y evalúa con `cierre=desconexion`. RH puede «Evaluar con lo que hay» (`POST /entrevistas/{codigo}/evaluar`, solo interrumpida/parcial con `turnosUtiles>0`, queda en bitácora con su nombre).
- Ficha del candidato: `MenuAcciones` abre hacia arriba si no cabe (el footer del modal tiene `overflow-hidden`); «Descartar candidato…» SIEMPRE en «…» con modal + motivo; `POST /decision descartar` con expediente abierto lo cancela en la misma transacción (motivo obligatorio; 409 solo si ya es alta). La pestaña «Expediente» (PanelContratacion) se muestra en Contratación Y Onboarding.
- Portal por Cuenta: `Cuenta.slug` (generado al arrancar por `seed.rellenar_slugs_cuentas` y al crear), `GET /vacantes/publicas?cuenta=<slug|id>` aísla la bolsa; sin parámetro es global (todas las Cuentas ACTIVAS) y cada fila trae `cuentaId/cuentaSlug`. `GET /vacantes/{codigo}` trae `homonimasOtrasCuentas` (mismo título publicado en otra Cuenta). Diagnóstico en prod: `scripts/diagnostico_vacantes_portal.py [texto]`. Regresión: `scripts/verificar_bugs_2026_09_17.py`.
- Recordatorios de documentos en 3 niveles (2026-09-17): `Expediente.recordatorios_enviados` es el ÚNICO contador (manuales desde el tablero de Onboarding / ficha del candidato y automáticos del job); `nivel_recordatorio` = min(enviados+1, 3) → `notificaciones.texto_recordatorio_documentos(nivel, …)` (1 ligero, 2 intermedio, 3 definitivo: firme, con consecuencia clara, nunca agresivo; `models.NIVELES_RECORDATORIO`). Tras el definitivo el job NO manda más (`recordatorios_agotados` una vez en bitácora; badge en Onboarding); RH puede seguir mandando a mano. Toda ruta que envíe un recordatorio pasa por `recordatorios.registrar_recordatorio_enviado`. Plantillas de Meta por nivel opcionales (`META_PLANTILLA_RECORDATORIO_2/_3`, `whatsapp.plantilla_documentos_por_nivel`); sin ellas la base. Regresión: `scripts/verificar_recordatorios_niveles.py`.
- Portal público (`app/portal/page.tsx`, 2026-09-17): buscador único + chips rápidos + panel de filtros avanzados (Estado→Municipio, área, empresa, seniority, sueldo, nuevas) + orden; TODOS los filtros viven en la URL (`filtrosAUrl`/`filtrosDesdeUrl`, liga compartible) junto con `?cuenta=`. Postularse sigue siendo un solo paso (`/aplicar/[slug]`); no agregar pantallas intermedias.
- UI (2026-09-17): `MenuAcciones` se pinta en un React Portal sobre `<body>` con posición fija (abre hacia arriba si no cabe; se cierra con scroll/resize/Escape/clic fuera) — ningún `overflow-hidden` de modales o tarjetas lo recorta; no volver a `absolute`. Sidebar del dashboard: en <1024px vive en un cajón deslizable (hamburguesa) con bloqueo de scroll y cierre al navegar; sin contadores quemados en la navegación. Modo TÓTEM de la sala `/entrevista/[token]`: `?totem=1` (o retrato ≥800×1400 detectado al montar) → pantalla completa, video `object-cover`, subtítulos grandes y tres controles enormes (Hablar/Silenciar micrófono · Sonido · Terminar); `?totem=0` lo desactiva. No re-evaluar el modo a mitad de sesión (desmontaría el <video>).

## WhatsApp multi-tenant (2026-09-17)

- UN número maestro atiende a TODAS las Cuentas activas por defecto. `webhooks._alcance_whatsapp` regresa (Cuentas, modo): `dedicado` solo si una Cuenta tiene `whatsapp_exclusivo=True` y su `whatsapp_comunicacion` = número receptor (opción Premium; único caso de ruteo por número); si no, `compartido` = todas las activas. Nunca elegir «la Cuenta más antigua».
- Persona = una fila `Candidato` POR CUENTA (`_personas_en_alcance`: la más reciente por Cuenta); `_resolver_postulacion` considera las postulaciones activas de todas sus filas. Menú: `_enviar_menu_vacantes` lista las Publicadas de todo el alcance en secciones por empresa (≤10 filas, límite de Meta); con más, primero pregunta la empresa (`CTA-<id>`, `analisis.eligiendo_empresa/cuenta_elegida`).
- Al elegir vacante, `_amarrar_a_cuenta`/`_persona_para_cuenta`: postulación + mensajes quedan en `vacante.cuenta_id`; la fila ancla se MUEVE de Cuenta si no tiene otro proceso, o se crea/reutiliza la fila de la persona en esa Cuenta. Toda identidad de empresa hacia el candidato sigue saliendo de la vacante (aislamiento). Las verificaciones que prueban ruteo por número deben marcar `whatsapp_exclusivo=True`. Regresión: `scripts/verificar_whatsapp_multitenant.py`.
- Móvil: menús horizontales (pestañas, chips) usan la utilidad `.scroll-x` (globals.css: overflow-x auto, nowrap, barra oculta, snap); los modales de pantalla completa en móvil (`h-[100dvh]` + `sm:max-h-[…]`).

## Despliegue 2026-09-18 (capacitación, RAG, validación, modo prueba, tiempo real, carta)

- Capacitación: el instructor con avatar convive con el contenido escrito (`components/capacitacion/instructor-avatar.tsx`; `POST /capacitacion/publica/{token}/sesion {modulo}` → Anam o texto, `POST …/turno` → `ia.curso_turno`; historial por módulo en `AsignacionCurso.transcript`). El contenido sigue siendo la fuente de verdad; el avatar nunca bloquea el avance. PDF del curso con fpdf2 (`services/pdf.pdf_curso`; `GET /capacitacion/publica/{token}/pdf` y `GET /capacitacion/{codigo}/pdf`). En fpdf2 SIEMPRE `multi_cell(..., new_x="LMARGIN", new_y="NEXT")` (si no, el siguiente multi_cell queda sin ancho).
- Base de conocimiento = RAG real (`services/rag.py`, `routers/conocimiento.py`, modelos `DocumentoConocimiento`/`FragmentoConocimiento`/`ConsultaConocimiento`): documentos por Cuenta → fragmentos (~900 chars, solape) → embeddings `text-embedding-3-small` (JSON) → coseno + empate léxico → respuesta ESTRUCTURADA (`RespuestaConocimiento`: respuesta, pasos, fuentes citadas, confianza, sin_evidencia) redactada SOLO con la evidencia. Sin clave: búsqueda léxica + respuesta extractiva. Nunca respuestas quemadas. Las consultas «sin evidencia» son políticas por documentar.
- Validación estricta de documentos: `DocumentoValidado.es_documento_oficial` + prompt de visión estricto; `contratacion._resolver_estado` RECHAZA si no coincide el tipo, no es documento oficial o `tipo_detectado` es otro/desconocido; solo la duda de titular va a revisión; sin IA nunca queda válido solo.
- Alta con Modo Prueba: `components/dashboard/switch-modo-prueba.tsx` (solo Administrador cambia; es `ConfiguracionSistema.modo_prueba`). ACTIVO → el alta se manda con `forzar_prueba=true` directo cuando el expediente <100%; INACTIVO → el botón queda deshabilitado hasta 100% validado (el backend lo refuerza: `puede_forzar_prueba`).
- Expediente en tiempo real: `PanelContratacion` hace `usePolling(cargarExpediente, 8000)` y refresca la ficha si cambió algún documento; el tablero de Onboarding ya revalidaba con `usePolling`.
- Carta de intención: PDF con el wordmark Red Human (`pdf.wordmark_red_human`, texto «Red» #58595b + «Human» #ee4444 + barra; fpdf2 no pinta el <text> del SVG) servido `inline`; se abre en la vista `/carta/[id]` (título + favicon de la app) — `urlCartaIntencion()` apunta ahí, `urlCartaIntencionPdf()` al PDF crudo.
- HOTFIX 2026-09-18 (arranque): las tablas de la Base de Conocimiento se crean en `migraciones.crear_tablas_conocimiento` (paso aparte, NO fatal; una por una) después de `crear_tablas_base`; si fallan, `rag.marcar_disponible(False, error)` → `/conocimiento/*` responde 503 con el motivo y el resto de la API arranca. Sus `cuenta_id` son enteros indexados SIN llave foránea a `cuentas` (el aislamiento lo da `cuenta_actual`); no volver a agregar FKs a `cuentas` en tablas nuevas sin validar el motor de prod. Diagnóstico: `scripts/diagnostico_esquema.py`.

## Notificaciones de Entrevista Humana (2026-09-18)

- Al agendar (`entrevista_agendada`) el entrevistador recibe por WhatsApp la plantilla de Meta `META_PLANTILLA_ENTREVISTA` (default `alerta_entrevista_asignada`) con EXACTAMENTE 6 parámetros posicionales en este orden: entrevistador, candidato, vacante, fecha, hora, liga al expediente (`notificaciones.parametros_plantilla_entrevista`; la liga es `/entrevista-humana/{token}`). Solo con `WHATSAPP_PROVIDER=meta`; si Meta la rechaza cae a texto libre con la liga (`whatsapp.enviar_plantilla_entrevista`, `motivo_fallback` visible a RH).
- Correos: `services/plantillas_correo.py` — `html_entrevistador(d, evento)` y `html_candidato(d, evento)` con `evento ∈ agendada|modificada|recordatorio|cancelada` (2026-09-18: los CUATRO eventos de la Entrevista Humana usan el mismo layout corporativo; cancelada sin CTA); el Cliente recibe `html_aviso` con el mismo layout. Ningún correo de entrevista sale en texto plano. Datos comunes: `notificaciones.datos_entrevista_humana` (se inyecta como `extra["_datos_entrevista"]` para los 4 eventos). Resend: sin clave se hace warning en log y el flujo sigue; nunca lanza.
- Vistas previas (sin sesión, sin enviar): `GET /api/emails/preview/{entrevistador|candidato}` — sin parámetros usa el ejemplo (`?modalidad=` solo cambia la variante); con query params (`evento, candidato, entrevistador, vacante, empresa, fecha ISO, hora, modalidad, liga, ubicacion, telefono, telefono_candidato, comentario, liga_expediente`) renderiza el correo EXACTO con datos reales. El modal «Programar entrevista humana» construye la URL con su estado en vivo (`lib/api.urlPreviewCorreo(plantilla, datos)`, iframe con `key=URL` → se recarga al cambiar fecha/hora/modalidad/liga); `postulacion_dict` expone `empresaVisible` para eso.
- Reglas: sin regla guardada, `REGLAS_NOTIFICACION_DEFAULT` es la BASE y el override parcial de la acción se aplica encima (antes un override parcial silenciaba todo lo no mencionado).

## Ajustes tras pruebas en producción (2026-09-18)

- Modo Prueba TOTAL (`ConfiguracionSistema.modo_prueba`): el alta (`contratacion.alta`) omite POR COMPLETO la integridad del expediente (documentos adjuntos, 100 %, confirmación de RH) sin necesidad de `forzar_prueba`; la subida de documentos (`_registrar_documento`) salta el OCR/IA y marca cualquier PDF/imagen como `recibido` con nota «Modo Prueba». Apagado → todo se exige como antes. Las verificaciones que prueben bloqueos deben apagar el Modo Prueba primero.
- Prompt de visión: los comprobantes de domicilio son válidos a nombre de terceros (`coincide_titular=null`); solo se juzga la legitimidad del comprobante.
- Entrevista Humana: el POST regresa `advertencias` (`notificaciones.advertencias_de`) cuando un correo/WhatsApp no salió; el frontend muestra un `Toast` amarillo (`components/dashboard/toast.tsx`, portal, esquina inferior) con «Entrevista asignada, pero el correo falló. Verifica la API Key o el Dominio». `correo.enviar_correo` etiqueta 401/403 como API Key inválida y 422/"domain" como dominio no verificado.
- Modal «Programar entrevista humana»: `sm:max-w-2xl`, cuerpo `overflow-y-auto` con `max-h-[85vh]`, footer fijo; botón «Ver cuerpo del correo» abre un sub-diálogo con iframes a `/api/emails/preview/{entrevistador|candidato}` (`lib/api.urlPreviewCorreo`).
- Colaborador eliminado (DELETE lógico) → se borran sus `AsignacionCurso` (y `Colaborador.asignaciones_curso` tiene `cascade="all, delete-orphan"` para el borrado físico); tablero, ficha del curso y contadores filtran por `AsignacionCurso.viva`.
- Curso asignado a un COLABORADOR (2026-09-18): WhatsApp de texto libre SIN plantilla (`whatsapp.enviar_texto_sin_plantilla`, `capacitacion.mensaje_curso_colaborador`: «Hola [Nombre], se te asignó el curso de capacitación de [Empresa]: [Título]. Accede a tu curso, interactúa con el avatar y descarga tu material en la siguiente liga: [URL]», URL absoluta `{app_url}/capacitacion/{token}`). Fuera de la ventana de 24 h NO se reintenta con plantilla: warning «Mensaje de texto rechazado por ventana de 24h» en el log, `fuera_de_ventana` en el resultado y la asignación se guarda igual (todo `_notificar` está en try/except). Candidatos/externos siguen con `enviar_mensaje`. Regresión: `scripts/verificar_whatsapp_curso_colaborador.py`.

## Bloque 1/2 — Entrevista Humana (2026-09-19, cambios Raúl)

- Correos: TODOS los eventos salen con el layout corporativo (`notificaciones._html` envuelve cualquier evento sin plantilla propia; `plantillas_correo.html_evaluacion_entrevistador`, `html_entrevista_completada(d, audiencia)`, `html_vacante_publicada`). Nunca `f"<p>{texto}</p>"`.
- Eventos nuevos: `vacante_publicada` (`notificaciones.notificar_vacante_publicada` al publicar: contactos del Cliente por regla + responsable; `POST /vacantes/{codigo}/publicar` es async y regresa `notificaciones`) y `entrevista_completada` (autocierre). `REGLAS_NOTIFICACION_DEFAULT` actualizadas (recordatorio a candidato+entrevistador por ambos canales).
- Recordatorio automático: `services/recordatorios_entrevista.py` cada 10 min, `ConfiguracionSistema.recordatorio_entrevista_horas` (default 24, 0 = apagado, editable en Configuración), reclama `EntrevistaHumana.recordatorio_enviado_en` antes de enviar.
- Liga del entrevistador (`GET /entrevista-humana/publica/{token}`): trae `expediente` (candidato, vacante, CV extraído, archivos vía `/archivo/{id}`, análisis de Luna, Entrevista Red Human, capacitación, documentos) y sigue abierta ya evaluada (`yaEvaluada`); el POST sigue rechazando una segunda evaluación. El frontend muestra primero el expediente (acordeones) y abajo el formulario.
- Autocierre: al evaluar desde la liga → `realizada=True`, `evaluada_en`, `resultado_capturado_por="entrevistador"`, evento `entrevista_completada` (candidato/cliente por regla) + `notificar_rh_entrevista_completada` (responsable de la vacante o `Cuenta.correo_comunicacion`). Comentarios SIEMPRE opcionales (liga y respaldo de RH).
- UI ficha: un solo paso — «Entrevista realizada» abre el único modal (Aprobado/Rechazado + comentarios opcionales + «Pedir segunda entrevista» + Guardar; recomendación derivada); «No realizada / Reprogramar» aparte; reenviar liga / recordatorio / modificar / cancelar en «…». No volver a agregar «¿La entrevista se llevó a cabo?».

## Bloque 3 — Contratación y alta (2026-09-19, cambios Raúl)

- Flujo lineal en la ficha (`PanelContratacion`): capturar condiciones (puesto, sueldo, tipo, fecha, ubicación, jefe, **empresa contratante** — `Expediente.empresa`, default la visible de la vacante — e instrucciones) → Guardar (`condiciones_guardadas_en`, `expedienteCondiciones.completas`) → en la MISMA pantalla: «Generar carta de intención», «Solicitar documentos», «Generar contrato» (habilitado solo con expediente al 100 % o Modo Prueba) y «Enviar a Onboarding».
- Documentos: vista previa en modal (iframe del PDF) con 3 acciones — WhatsApp (liga pública `/expediente/{token}` con la carta descargable: `GET /expedientes/publica/{token}/carta-intencion`), Correo (HTML corporativo + PDF adjunto; `correo.enviar_correo(adjuntos=[{filename, content}])`) y Descargar. `POST /contratacion/expedientes/{id}/carta-intencion/enviar {canal}`; `GET /contratacion/expedientes/{id}/contrato` (`pdf.pdf_contrato`, 409 si el expediente no está al 100 %). Trazabilidad solo en bitácora (`carta_intencion_enviada`, `contrato_generado`).
- Alta perfecta: `contratacion.alta` exige (salvo Modo Prueba) documentos adjuntos Y condiciones guardadas (puesto, sueldo, tipo, fecha). `_crear_colaborador` toma ESTRICTAMENTE el expediente (empresa visible de la vacante solo como default) y guarda `Colaborador.tipo_contratacion` + `Colaborador.condiciones_ingreso` (snapshot JSON inmutable con quién/cuándo). Nunca volver a usar `vac.empresa` (nombre interno) ni `vac.sueldo` como fuente del colaborador. Regresión: `scripts/verificar_bloque3_contratacion.py`.

## Bloque 4 — Capacitación (2026-09-19, cambios Raúl)

- Antes de generar se pregunta la modalidad (`Curso.modalidad` ∈ `MODALIDADES_CURSO`: `instructor_ia` | `autoguiado`) y la duración LIBRE (`Curso.duracion_texto`, «5 min», «1 h 30»); `duracion_horas` se deriva con `ia.horas_desde_texto` solo para KPIs. `POST /capacitacion/generar` acepta `duracion` (texto) + `modalidad` (y `duracion_horas` por compatibilidad).
- `ia.guion_curso(..., modalidad, duracion_texto)`: Instructor IA → guion CONVERSACIONAL (frases cortas, lenguaje natural, pausas para preguntas); Autoguiado → contenido breve, visual, modular. Cada módulo trae `resumen` + `puntos_clave` (`ModuloCurso.resumen/puntos_clave`): el PDF es MATERIAL DE APOYO (en Instructor IA lleva resumen + puntos clave, no el guion hablado).
- Evaluaciones: el prompt exige `correcta` = única opción verdadera y `explicacion` que justifique ESA opción de ESA pregunta; `ia._verificar_evaluacion` hace una segunda pasada resolviendo cada pregunta de forma independiente y corrige índice/explicación. UX sigue siendo una pregunta por pantalla.
- Nomenclatura: «Finalizar curso» (Crear → Revisar → Finalizar → Asignar). `PATCH /capacitacion/{codigo}/finalizar` (alias `/publicar`); el estado interno sigue siendo «Publicado» (`etiquetaEstadoCurso` → «Finalizado»).
- Sala pública: con `instructor_ia` el avatar arranca solo (`InstructorAvatar autoIniciar`), el guion queda plegado («Ver el guion del módulo») y el flujo es explica → pregunta → Continuar → siguiente módulo → evaluación; con `autoguiado` se lee el contenido y el instructor queda disponible por texto al lado. Regresión: `scripts/verificar_bloque4_capacitacion.py`.
